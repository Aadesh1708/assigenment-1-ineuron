#include <stdio.h>
#include <string.h>

int main() 
{
   char String[1000];
   int i;
   printf("Enter the string: ");
   scanf("%s", String);
   for(i=0;String[i]!='\0';++i);
   printf("Lemgth of string is %d",i);
   return 0;
}
